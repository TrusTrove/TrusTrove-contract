# Stellar network for deployment
STELLAR_NETWORK=testnet
STELLAR_RPC_URL=https://soroban-testnet.stellar.org
STELLAR_NETWORK_PASSPHRASE="Test SDF Network ; September 2015"
HORIZON_URL=https://horizon-testnet.stellar.org

# Deployer key alias (created with: stellar keys generate deployer --network testnet --fund)
DEPLOYER_ACCOUNT=deployer

# USDC on Stellar testnet
USDC_ISSUER=GBBD47IF6LWK7P7MDEVSCWR7DPUWV3NY3DTQEVFL4NAT4AQH3ZLLFLA5
USDC_ASSET_CODE=USDC

# XLM (native asset on Stellar) - EXPERIMENTAL (incomplete setup, not supported in frontend)
XLM_ASSET=CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHQCYSC

# Populated after deploy.sh runs — paste into trusttrove-app .env.local
REGISTRY_CONTRACT_ID=
INVOICE_CONTRACT_ID=
ESCROW_USDC_CONTRACT_ID=
ESCROW_XLM_CONTRACT_ID=   # EXPERIMENTAL: Incomplete setup, not supported in frontend
POOL_USDC_CONTRACT_ID=
POOL_XLM_CONTRACT_ID=     # EXPERIMENTAL: Incomplete setup, not supported in frontend

# Agent-registry contract from the separate underwrite-contract repo.
# Populate once it's deployed, then call invoice's set_agent_registry_contract
# with this address. This wiring step is manual and out of scope for
# deploy.sh/deploy.ps1 — see "Agent Registry Wiring" in DEPLOYMENT.md.
AGENT_REGISTRY_CONTRACT=

